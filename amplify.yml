version: 1
frontend:  
  phases:    
    preBuild:      
      commands:        
        - npm ci
        - pip3 install --user pipenv virtualenv
        - pipenv install
    build:
      commands:  
        - npm install --save      
        - npm run build  
  artifacts:    
    baseDirectory: dist    
    files:      
      - '**/*'  
  cache:    
    paths:      
      - node_modules/**/*